<view class="listTab">
    <view class="listTab-qh {{index === activeIndex?'actave':''}}" 
    wx:for="{{tabs}}" 
    wx:key="id" 
    bindtap="listDianji"
    data-list="{{index}}"
    >
        <text class="listTab-text">{{item.title}}</text>
    </view>
</view>
  
